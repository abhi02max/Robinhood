export const TECHNICAL_QUESTIONS = [
  // ─── DSA & Algorithms (12) ───
  {
    id: 'tech-dsa-001',
    category: 'technical',
    subcategory: 'dsa',
    title: 'HashMap — Time Complexity Analysis',
    question: 'Walk me through the average and worst-case time complexity of HashMap get/put operations. When does O(1) degrade, and how do modern implementations handle it?',
    idealAnswer: 'HashMap provides O(1) average-case for get and put via hash function mapping to a bucket index. Worst case degrades to O(n) when all keys hash to the same bucket (chain collision). Java 8+ mitigates this by converting long linked list chains (8+ elements) to balanced red-black trees, improving the worst-case to O(log n). The load factor (default 0.75) triggers rehashing / array resizing to maintain performance. For thread safety, ConcurrentHashMap uses segment-level locking or CAS operations rather than synchronizing the whole map.',
    keyPoints: [
      'O(1) average for get/put via hashing',
      'O(n) worst case due to hash collisions',
      'Java 8+ tree-ifies long chains → O(log n)',
      'Load factor (0.75) triggers rehashing/resizing',
      'ConcurrentHashMap for thread safety / granular locking',
    ],
    followUps: [
      'How would you design a custom hash function to minimize collisions?',
      'Compare HashMap vs TreeMap — when would you choose each?',
    ],
    difficulty: 'Medium',
    companies: ['google', 'amazon', 'microsoft', 'meta', 'apple'],
    roles: ['sde', 'backend', 'fullstack'],
    tags: ['hashmap', 'complexity', 'data-structures', 'collision', 'java'],
    xpReward: 15,
  },
  {
    id: 'tech-dsa-002',
    category: 'technical',
    subcategory: 'dsa',
    title: 'Two Pointers vs Sliding Window',
    question: 'Explain the difference between the Two Pointers and Sliding Window techniques. Give examples of problems where each is the optimal approach.',
    idealAnswer: 'Two Pointers involves two indices traversing a data structure, often moving towards each other (e.g., meeting in the middle) or moving in the same direction at different speeds. It is ideal for sorted arrays (e.g., Two Sum on sorted array, checking Palindrome). Sliding Window is a subset of Two Pointers where the two pointers define a continuous subarray or "window" that moves through the data. It expands and contracts to satisfy a condition (e.g., Longest Substring Without Repeating Characters, Maximum Sum Subarray of size K). Sliding Window is used primarily for contiguous sequences, while Two Pointers can be used for discrete pairs or sorted data.',
    keyPoints: [
      'Two pointers move towards each other or at different speeds',
      'Useful for sorted arrays or cycle detection',
      'Sliding window defines a continuous subarray',
      'Window expands/contracts to meet conditions',
      'Examples: Two Sum (Pointers) vs Longest Substring (Window)',
    ],
    followUps: [
      'How does the fast & slow pointer (Tortoise and Hare) algorithm detect loops?',
      'How do you handle a sliding window problem with negative numbers?',
    ],
    difficulty: 'Easy',
    companies: ['amazon', 'microsoft', 'uber', 'stripe'],
    roles: ['sde', 'frontend', 'backend', 'fullstack'],
    tags: ['two-pointers', 'sliding-window', 'array', 'string', 'algorithms'],
    xpReward: 10,
  },
  {
    id: 'tech-dsa-003',
    category: 'technical',
    subcategory: 'dsa',
    title: 'Binary Search Limitations',
    question: 'Binary search is O(log n) on perfectly sorted data. What happens if the data is rotated (like [4,5,6,7,0,1,2])? Walk me through how you adapt binary search for a rotated sorted array.',
    idealAnswer: 'When an array is rotated, it consists of two sorted subarrays. The key adaptation is to determine which half of the array is perfectly sorted at each step. By comparing the middle element `mid` with `left`, we can see if the left half is strictly increasing. If `left <= mid`, the left half is sorted. We then check if our target falls within the range `[left, mid]`. If it does, we search the left half; otherwise, we search the right half. If the right half is sorted (`mid < right`), we similarly check if the target lies within `[mid, right]`. This maintains the O(log n) time complexity by eliminating half the search space consistently.',
    keyPoints: [
      'Array splits into two separately sorted halves',
      'Compare mid with left/right to find the contiguous sorted half',
      'Check if target falls within the sorted boundary',
      'Eliminate half the search space based on boundary checks',
      'Maintains O(log n) time complexity',
    ],
    followUps: [
      'What if the rotated sorted array contains duplicates? How does that affect time complexity?',
      'How would you find the minimum element in a rotated sorted array?',
    ],
    difficulty: 'Medium',
    companies: ['meta', 'google', 'amazon', 'apple'],
    roles: ['sde', 'backend', 'frontend'],
    tags: ['binary-search', 'array', 'algorithms', 'rotated'],
    xpReward: 15,
  },
  {
    id: 'tech-dsa-004',
    category: 'technical',
    subcategory: 'dsa',
    title: 'DFS vs BFS — Recursion and Queues',
    question: 'Compare Depth First Search (DFS) and Breadth First Search (BFS) for traversing graphs/trees. How are they implemented, and when would you choose one over the other?',
    idealAnswer: 'DFS explores as deep as possible along each branch before backtracking. It is typically implemented using recursion (call stack) or an explicit Stack. It is highly memory efficient for deep trees (O(H) space) and ideal for finding paths, topological sorting, and solving mazes. BFS explores level by level (node neighbors first). It is implemented using a Queue. It requires more memory for wide trees (O(W) space where W is max width) but is guaranteed to find the shortest path in unweighted graphs. Choose BFS for shortest path or traversing nearby nodes; choose DFS for exhaustive search, cycle detection, or deep property checks.',
    keyPoints: [
      'DFS goes deep before backtracking; uses Stack/Recursion',
      'BFS explores level-by-level; uses Queue',
      'DFS space is O(h) (height), BFS space is O(w) (width)',
      'BFS guaranteed to find shortest path in unweighted graphs',
      'DFS preferred for topological sort, cycle detection, or memory constraints',
    ],
    followUps: [
      'How do you manage visited nodes in a graph with cycles?',
      'What is Iterative Deepening DFS and why use it?',
    ],
    difficulty: 'Easy',
    companies: ['amazon', 'google', 'microsoft', 'uber', 'meta'],
    roles: ['sde', 'backend', 'fullstack'],
    tags: ['graph', 'tree', 'dfs', 'bfs', 'algorithms'],
    xpReward: 10,
  },
  {
    id: 'tech-dsa-005',
    category: 'technical',
    subcategory: 'dsa',
    title: 'Dynamic Programming — Top-Down vs Bottom-Up',
    question: 'Explain the concepts of Top-Down (Memoization) and Bottom-Up (Tabulation) in Dynamic Programming. Which one is generally preferred and why?',
    idealAnswer: 'Dynamic Programming solves overlapping subproblems. Top-Down (Memoization) uses recursion and caches the result of each subproblem as it is computed. It is intuitive since it mirrors the recursive formulation and only computes required subproblems. Bottom-Up (Tabulation) uses iteration and builds an table incrementally from base cases up to the final answer. Bottom-Up is generally preferred in production because it avoids the overhead of recursive function calls and eliminates the risk of StackOverflow errors. Additionally, Bottom-Up often allows for space optimization, reducing an O(N) array to O(1) space if only the previous few states are needed (like in Fibonacci or Climbing Stairs).',
    keyPoints: [
      'Top-Down uses recursion + cache (Memoization)',
      'Bottom-Up uses iteration + table (Tabulation)',
      'Top-Down only computes necessary subproblems',
      'Bottom-Up prevents StackOverflow and recursion overhead',
      'Bottom-Up often allows state-space optimization (e.g., O(1) space)',
    ],
    followUps: [
      'Give an example of state-space optimization in the Knapsack problem.',
      'When would Top-Down be strictly better than Bottom-Up?',
    ],
    difficulty: 'Medium',
    companies: ['google', 'meta', 'amazon', 'apple'],
    roles: ['sde', 'backend', 'data'],
    tags: ['dynamic-programming', 'algorithms', 'memoization', 'tabulation'],
    xpReward: 15,
  },
  {
    id: 'tech-dsa-006',
    category: 'technical',
    subcategory: 'dsa',
    title: 'Trie (Prefix Tree) Applications',
    question: 'What is a Trie data structure? Discuss its time complexity and scenarios where it outperforms a typical HashSet or Binary Search Tree.',
    idealAnswer: 'A Trie is a tree-like data structure used to efficiently store and retrieve strings, where each node represents a character. Time complexity for insert and search is O(L), where L is the length of the word, making it independent of the number of words stored (unlike BST which is O(log N) string comparisons). Scenarios where a Trie significantly outperforms a HashSet include prefix matching, autocomplete/typeahead features, spell checkers, and longest prefix matching (e.g., IP routing). While a HashSet can perform exact matches in O(1) time, it cannot easily find all words starting with a specific prefix.',
    keyPoints: [
      'Tree structure where edges/nodes represent characters',
      'Insert and Search time complexity is O(L) where L is string length',
      'Excellent for prefix matching and autocomplete features',
      'Does not require string hashing collisions resolution',
      'Consumes more memory (pointer overhead) than a flattened array or map',
    ],
    followUps: [
      'How would you optimize the memory footprint of a Trie?',
      'Explain how a Radix Tree (Compressed Trie) differs from a standard Trie.',
    ],
    difficulty: 'Medium',
    companies: ['twitter', 'stripe', 'amazon', 'microsoft'],
    roles: ['sde', 'backend', 'frontend'],
    tags: ['trie', 'data-structures', 'string', 'prefix'],
    xpReward: 15,
  },
  {
    id: 'tech-dsa-007',
    category: 'technical',
    subcategory: 'dsa',
    title: 'Detecting Cycles in a Linked List',
    question: 'How do you detect a cycle in a singly linked list in O(1) space? Explain the intuition behind the algorithm and how to find the exact node where the cycle begins.',
    idealAnswer: 'We use Floyd’s Cycle-Finding Algorithm (Tortoise and Hare). We initialize two pointers: slow moves 1 step, fast moves 2 steps. If there is a cycle, the faster pointer will eventually lap the slower one and they will meet inside the cycle. This proves the cycle exists using O(1) space and O(N) time. To find the exact start node of the cycle: once they meet, we move one pointer back to the head of the list. Then, we move both pointers one step at a time. The point where they meet again is mathematically guaranteed to be the start of the cycle.',
    keyPoints: [
      'Use Floyd’s Tortoise and Hare algorithm (Fast and Slow pointers)',
      'Fast pointer moves 2 steps, slow pointer moves 1 step',
      'Meeting point inside the list confirms a cycle exists',
      'O(N) time complexity and O(1) auxiliary space',
      'Reset one pointer to head and move both by 1 step to find cycle start',
    ],
    followUps: [
      'What is the mathematical proof that the two pointers will meet at the start of the cycle?',
      'How else could you find a cycle if O(N) space were allowed?',
    ],
    difficulty: 'Easy',
    companies: ['microsoft', 'apple', 'amazon', 'uber'],
    roles: ['sde', 'backend', 'fullstack'],
    tags: ['linked-list', 'two-pointers', 'cycle-detection'],
    xpReward: 10,
  },
  {
    id: 'tech-dsa-008',
    category: 'technical',
    subcategory: 'dsa',
    title: 'Top K Elements — Min-Heap vs Quickselect',
    question: 'You need to find the Kth largest element in an unsorted array of N items. Compare using a Sorting approach, a Min-Heap approach, and Quickselect. Which is optimal for processing continuous streams?',
    idealAnswer: 'Sorting the full array takes O(N log N) time, which is inefficient if K is small. A Min-Heap of size K takes O(N log K) time and O(K) space. We iterate through elements, adding them to the heap and popping the minimum if size exceeds K; the root will hold the Kth largest element. Quickselect averages O(N) time by using partitioning (like Quicksort) but only recurring into the partition containing the target rank. Worst-case is O(N²), but expected is O(N) with O(1) space.\n\nFor a continuous data stream, the Min-Heap is strictly optimal. Quickselect requires the entire dataset in memory at once, whereas the heap maintains the top K elements incrementally as new single items arrive.',
    keyPoints: [
      'Sorting is O(N log N) which is overkill',
      'Min-Heap of size K is O(N log K) time and O(K) space',
      'Quickselect averages O(N) time but worst case O(N²)',
      'Quickselect is an in-place algorithm (requires full array)',
      'Min-Heap is the only viable option for continuous streaming data',
    ],
    followUps: [
      'How do you prevent the worst-case O(N²) time complexity in Quickselect?',
      'How does keeping a Max-Heap instead of a Min-Heap change the logic?',
    ],
    difficulty: 'Hard',
    companies: ['facebook', 'amazon', 'google', 'netflix'],
    roles: ['sde', 'backend', 'data'],
    tags: ['heap', 'sorting', 'quickselect', 'streaming'],
    xpReward: 25,
  },
  {
    id: 'tech-dsa-009',
    category: 'technical',
    subcategory: 'dsa',
    title: 'Dijkstra vs Bellman-Ford',
    question: 'Comparing Shortest Path Graph Algorithms: When would you use Dijkstra’s algorithm and when would you use Bellman-Ford? What are their time complexities?',
    idealAnswer: 'Dijkstra’s algorithm finds the shortest path from a source to all other nodes optimally using a Min-Priority Queue. Its time complexity is O((V+E) log V). However, Dijkstra’s assumes edge weights are non-negative; it fails if the graph has negative edge weights because its greedy nature locks a node’s distance once extracted from the heap.\n\nBellman-Ford is used when the graph contains negative edge weights. It works by relaxing all edges V-1 times. It takes O(V × E) time, making it slower than Dijkstra. Furthermore, Bellman-Ford is the standard algorithm to detect negative weight cycles in a graph (by running relaxation one extra time and checking for further optimizations).',
    keyPoints: [
      'Dijkstra uses Priority Queue, greedy approach O((V+E) log V)',
      'Dijkstra fails on negative edge weights',
      'Bellman-Ford handles negative weights, O(V × E) time',
      'Bellman-Ford iteratively relaxes edges V-1 times',
      'Bellman-Ford can detect negative weight cycles',
    ],
    followUps: [
      'What algorithm finds all-pairs shortest paths? (Floyd-Warshall)',
      'Why exactly does Dijkstra fail on negative weights?',
    ],
    difficulty: 'Medium',
    companies: ['google', 'uber', 'lyft', 'microsoft'],
    roles: ['sde', 'backend', 'data'],
    tags: ['graph', 'shortest-path', 'dijkstra', 'algorithms'],
    xpReward: 15,
  },
  {
    id: 'tech-dsa-010',
    category: 'technical',
    subcategory: 'dsa',
    title: 'LRU Cache Implementation',
    question: 'How do you design a Least Recently Used (LRU) Cache that provides O(1) time complexity for both `get` and `put` operations?',
    idealAnswer: 'An LRU Cache requires fast lookups and fast eviction. To achieve O(1) time complexity, we combine a Hash Map and a Doubly Linked List. \n\nThe Hash Map stores the key mapped to the memory address of a Node in the Doubly Linked List. This provides O(1) access to any cached element. \nThe Doubly Linked List maintains the order of usage. The most recently used items are kept near the head, and the least recently used fall to the tail. \n\nWhen a `get` occurs, the node is isolated via tail/head pointers and moved to the head. When a `put` occurs, the node is inserted at the head. If the cache is full, the tail node is removed from both the Linked List and the Hash Map in O(1) time.',
    keyPoints: [
      'Requires combining Hash Map and Doubly Linked List',
      'Hash Map stores Key → Pointer to List Node for O(1) lookup',
      'Doubly Linked List tracks usage order (Head = MRU, Tail = LRU)',
      'Get/Put moves accessed items directly to the list HEAD',
      'Eviction involves dropping the TAIL node and removing its map key',
    ],
    followUps: [
      'Why is a Singly Linked List insufficient for this structure?',
      'How would you implement an LFU (Least Frequently Used) cache?',
    ],
    difficulty: 'Hard',
    companies: ['amazon', 'microsoft', 'bloomberg', 'meta'],
    roles: ['sde', 'backend', 'system-design'],
    tags: ['design', 'cache', 'hashmap', 'linked-list'],
    xpReward: 25,
  },
  {
    id: 'tech-dsa-011',
    category: 'technical',
    subcategory: 'dsa',
    title: 'Bit Manipulation — Finding the Missing Number',
    question: 'In an array containing `n` distinct numbers taken from `0, 1, 2, ..., n`, one number is missing. Describe how you would find the missing number in O(n) time and O(1) extra space. Why use BitManipulation over Math?',
    idealAnswer: 'There are two common O(1) space solutions. The Math approach calculates the expected sum of 1 to n using the formula `n*(n+1)/2` and subtracts the actual sum of the array. The delta is the missing number. However, if `n` is extremely large, calculating the mathematical sum can cause Integer Overflow.\n\nThe safer, purely O(N) approach uses Bit Manipulation (XOR). We know that `X XOR X = 0` and `X XOR 0 = X`. If we XOR all indices `0` through `n`, and also XOR all the values actually present in the array, every number that appears in both groups will cancel itself out. The only remaining value after the complete XOR pass is the missing number. This operation never overflows and completes in one pass.',
    keyPoints: [
      'Math approach: Sum formula (n*(n+1)/2) - Actual sum',
      'Math approach risks integer overflow for very large N',
      'Bitwise approach: XOR all indices and XOR all array values',
      'Properties of XOR: X ^ X = 0 and X ^ 0 = X',
      'XOR approach is safer, robust, O(N) time, and strictly O(1) space',
    ],
    followUps: [
      'What if the array is sorted? Does an O(log N) solution exist?',
      'How would you find a single missing number if TWO numbers were missing?',
    ],
    difficulty: 'Easy',
    companies: ['apple', 'adobe', 'microsoft', 'startups'],
    roles: ['sde', 'backend'],
    tags: ['bit-manipulation', 'math', 'array', 'algorithms'],
    xpReward: 10,
  },
  {
    id: 'tech-dsa-012',
    category: 'technical',
    subcategory: 'dsa',
    title: 'Merge Sort vs Quick Sort',
    question: 'Compare Merge Sort and Quick Sort. While both have O(n log n) average time complexity, Quick Sort is usually preferred in practice, but Merge Sort is used for linked lists and stable sorting. Explain why.',
    idealAnswer: 'Quick Sort uses pivoting to partition data. Its average time is O(n log n) with a very small constant factor, making it faster in practice due to contiguous memory access and excellent spatial locality (CPU cache friendliness). However, its worst-case is O(n²) if poorly pivoted (e.g., already sorted data), and it is not a stable sort.\n\nMerge Sort consistently guarantees O(n log n) time and is a Stable Sort (preserves order of equal elements). It requires O(n) extra space for arrays, which is a drawback. However, for Linked Lists, Merge Sort operates in O(1) extra space by simply changing pointers, rather than moving large chunks of memory. Thus, Quick Sort is best for in-memory arrays, while Merge Sort is best for stable sorting, external sorting (huge datasets), and linked structures.',
    keyPoints: [
      'Quick Sort is highly cache-efficient and faster in practical array sorting',
      'Quick Sort is In-Place O(1) auxiliary space, but Unstable',
      'Quick Sort worst-case is O(N²)',
      'Merge Sort is strictly O(N log N) worst-case and Stable',
      'Merge Sort requires O(N) space for arrays, but O(1) space for Linked Lists',
    ],
    followUps: [
      'How does Java/Python sort data under the hood? (Timsort/Dual-Pivot QuickSort)',
      'How would you sort a 1-Terabyte file using 2GB of RAM?',
    ],
    difficulty: 'Medium',
    companies: ['google', 'amazon', 'stripe', 'atlassian'],
    roles: ['sde', 'backend', 'fullstack'],
    tags: ['sorting', 'merge-sort', 'quick-sort', 'algorithms'],
    xpReward: 15,
  },

  // ─── System Design (9) ───
  {
    id: 'tech-sys-001',
    category: 'technical',
    subcategory: 'system-design',
    title: 'CAP Theorem and Database Tradeoffs',
    question: 'Explain the CAP Theorem when designing distributed systems. How do AP (Availability/Partition-Tolerance) systems like Cassandra handle state reconciliation compared to CP (Consistency/Partition-Tolerance) databases?',
    idealAnswer: 'The CAP Theorem states that a distributed data store can gracefully provide only two of three guarantees: Consistency (every read receives the most recent write), Availability (every request receives a response), and Partition Tolerance (system continues despite network drops). Because network partitions (P) are unavoidable in distributed systems, we must choose between CP and AP.\n\nCP databases (like Zookeeper, MongoDB, HBase) sacrifice availability by locking data or returning errors when separated to ensure strict consistency. AP databases (like Cassandra, DynamoDB) remain highly available, accepting writes across decoupled nodes. To resolve the eventual inconsistencies, AP systems use eventual consistency mechanisms like Read Repair or Anti-Entropy with Gossip protocols, determining the true state via vector clocks or last-write-wins (LWW) timestamps.',
    keyPoints: [
      'CAP guarantees: Consistency, Availability, Partition Tolerance',
      'Partitions cannot be avoided; choice is strictly CP vs AP',
      'CP systems reject writes or lock partitions to guarantee true state',
      'AP systems accept writes anywhere, leading to Eventual Consistency',
      'AP systems reconcile data using Gossip protocols, Vector Clocks, or LWW',
    ],
    followUps: [
      'Explain PACELC and how it extends the CAP theorem.',
      'Why is RDBMS considered CA traditionally, and why does that fail at scale?',
    ],
    difficulty: 'Medium',
    companies: ['amazon', 'uber', 'meta', 'netflix'],
    roles: ['backend', 'system-design', 'data'],
    tags: ['database', 'distributed-systems', 'cap-theorem', 'theory'],
    xpReward: 15,
  },
  {
    id: 'tech-sys-002',
    category: 'technical',
    subcategory: 'system-design',
    title: 'Designing a Rate Limiter',
    question: 'Walk me through the design of a distributed Rate Limiter for a public API (e.g., 100 requests per minute). What algorithms can be used, and how do you ensure the rate limit works accurately across multiple microservice instances?',
    idealAnswer: 'A distributed Rate Limiter guards APIs against abuse. Common algorithms include Token Bucket (allows bursts, refills linearly), Leaking Bucket (strict continuous rate), Fixed Window (simple but suffers from burst-at-boundary problems), and Sliding Window Log/Counter (smooth but takes more memory).\n\nTo work across distributed microservices, the rate limit state must be centralized or synced. A centralized approach uses a fast in-memory datastore like Redis. We store client IPs or API keys mapped to their quota counters. To avoid race conditions across pods reading/writing to Redis simultaneously, we must use Atomic Operations (like `INCR` and `EXPIRE`), or evaluate the check inside Lua scripts on the Redis server itself. Caching edge limits locally in the pods for 1-2 seconds can improve performance if strict precision isn`t mandatory.',
    keyPoints: [
      'Identify scaling algorithms: Token Bucket, Fixed Window, Sliding Window',
      'State must be shared/centralized across distributed pods (e.g., Redis)',
      'Uses atomic operations (INCR) or Lua scripts to prevent Race Conditions',
      'Use fast lookup keys: IP address, API Key, or User ID',
      'Can return HTTP 429 Too Many Requests cleanly',
    ],
    followUps: [
      'What are the downsides to using Redis for a global rate limiter at extreme, massive scale?',
      'How does the Token Bucket algorithm handle request bursts?',
    ],
    difficulty: 'Medium',
    companies: ['stripe', 'discord', 'cloudflare', 'atlassian'],
    roles: ['backend', 'system-design'],
    tags: ['system-design', 'api', 'rate-limiter', 'redis'],
    xpReward: 15,
  },
  {
    id: 'tech-sys-003',
    category: 'technical',
    subcategory: 'system-design',
    title: 'SQL vs. NoSQL at Scale',
    question: 'When architecting a new microservice, what specific parameters would cause you to choose a NoSQL database (like MongoDB/DynamoDB) over an SQL database (like PostgreSQL)?',
    idealAnswer: 'The decision between SQL and NoSQL centers on schema flexibility, scaling mechanics, and access patterns. \n\nI choose SQL (PostgreSQL/MySQL) when we have strictly structured, relational data requiring ACID compliance, complex JOIN operations, or financial transactions. SQL scales vertically well but has high operational costs for heavy horizontal sharding.\n\nI choose NoSQL (DynamoDB/Cassandra) parameter conditions include: 1) The application requires massive write throughput that exceeds a single node. 2) The schema is highly dynamic, unstructured, or evolving quickly. 3) The queries are simple key-value lookups without complex JOIN operations. NoSQL scales horizontally across clusters out-of-the-box by partitioning data using partition keys. However, it sacrifices strong ACID transactions across multiple partitions, guaranteeing only eventual consistency.',
    keyPoints: [
      'SQL for relational data, ACID compliance, complex JOINs',
      'SQL scales vertically; horizontal scaling (sharding) is difficult',
      'NoSQL for massive horizontal write throughput',
      'NoSQL for dynamic, flexible schemas or JSON doc storage',
      'NoSQL struggles with complex queries; relies on Base/Eventual consistency',
    ],
    followUps: [
      'How do you model One-to-Many relationships in DynamoDB without JOINs?',
      'What is horizontal sharding in an RDBMS?',
    ],
    difficulty: 'Easy',
    companies: ['startups', 'amazon', 'google', 'meta'],
    roles: ['backend', 'sde', 'fullstack'],
    tags: ['database', 'sql', 'nosql', 'system-design'],
    xpReward: 10,
  },
  {
    id: 'tech-sys-004',
    category: 'technical',
    subcategory: 'system-design',
    title: 'Designing a URL Shortener',
    question: 'Walk me through designing a URL Shortener system like bit.ly. Specifically, explain how you would generate the short alias, handle massive read throughput, and manage collisions.',
    idealAnswer: 'The core of a URL shortener is generating a unique short alias for long URLs. \n\nApproach 1: MD5 Hash the URL, convert to Base62 (A-Z, a-z, 0-9), and truncate to 7 characters. Since truncation causes collisions, we check the DB on insertion; if a collision occurs, we append a salt/counter and retry.\nApproach 2 (Better for scale): Use a centralized ID generator (like Snowflake or Zookeeper) that dispenses unique 64-bit integers. We Base62 encode that integer to yield a completely unique 7-character string. There are zero collisions in this approach.\n\nFor massive read throughput (100:1 read/write ratio), we place an LRU Cache (Redis/Memcached) in front of the database to store the mappings. Additionally, we horizontally scale the web stateless tier and use a NoSQL database (DynamoDB/Cassandra) mapped `short_url -> long_url` for rapid lookup, optionally configured with TTLs for link expiration.',
    keyPoints: [
      'Base62 encoding logic (A-Z, a-z, 0-9 yields 62 characters)',
      'Address collisions via centralized unique ID generation (Snowflake/Ticket Service)',
      'Highly read-heavy system -> Requires intensive Caching (Redis)',
      'Datastore choices: Key-Value NoSQL aligns perfectly with use case',
      'Implement TTLs / background jobs for expired links to save storage space',
    ],
    followUps: [
      'If two users shorten the identical long URL, do they receive the same short URL?',
      'How do you scale the ID generator to avoid a single point of failure?',
    ],
    difficulty: 'Hard',
    companies: ['meta', 'google', 'twitter', 'lyft'],
    roles: ['backend', 'system-design'],
    tags: ['system-design', 'hld', 'base62', 'cache'],
    xpReward: 25,
  },
  {
    id: 'tech-sys-005',
    category: 'technical',
    subcategory: 'system-design',
    title: 'Microservices Communication Patterns',
    question: 'In a microservice architecture, how do you decide between synchronous communication (REST/gRPC) versus asynchronous communication (Message Brokers / Event Bus)? What are the risks of using synchronous calls everywhere?',
    idealAnswer: 'Synchronous communication (REST/gRPC) implies the caller waits for a response. It is ideal for read operations, user-facing queries, or tasks where immediate confirmation is mandatory (e.g., fetching a user profile, validating a login). \n\nAsynchronous communication (Kafka, RabbitMQ, SQS) uses publish-subscribe or message queues. It is ideal for heavy background processing, decoupled system reactions (e.g., sending welcome emails after registration), and smoothing out massive traffic spikes via queuing.\n\nThe massive risk of using Synchronous calls everywhere is cascade failures and latency amplification. If Service A calls B, which calls C, the total latency is compounded. If C goes down, A and B also block and fail. Asynchronous event patterns decouple this, so if the Email service is down, the Registration service still successfully queues the task and continues functioning.',
    keyPoints: [
      'Synchronous (REST/gRPC) blocks and waits; good for immediate reads',
      'Asynchronous (Message Queues) fires-and-forgets; good for heavy/decoupled work',
      'Synchronous chains cause cascading failures and compounded latency',
      'Asynchronous queues handle traffic spikes seamlessly (load leveling)',
      'Event-Driven architectures require handling idempotent processing',
    ],
    followUps: [
      'In a microservices setup, how do you handle distributed transactions where a failure requires rolling back changes in 3 different services? (Saga Pattern)',
      'What is an API Gateway, and what role does it play here?',
    ],
    difficulty: 'Medium',
    companies: ['netflix', 'uber', 'amazon', 'adobe'],
    roles: ['backend', 'system-design'],
    tags: ['microservices', 'grpc', 'kafka', 'architecture'],
    xpReward: 15,
  },
  {
    id: 'tech-sys-006',
    category: 'technical',
    subcategory: 'system-design',
    title: 'Caching Strategies',
    question: 'Explain the difference between Cache-Aside, Read-Through, and Write-Through caching strategies. In what scenarios does cache-invalidation become complicated?',
    idealAnswer: 'Cache-Aside: The application searches the cache. If it misses, the application fetches from DB and writes to cache itself. It’s flexible but code-heavy.\nRead-Through: Application asks the cache for data. If missing, the cache provider automatically fetches it from the DB. This simplifies app code but requires a cache integrated with the datastore.\nWrite-Through: App writes data to the cache, which synchronously pushes it to the DB. Reads are exceptionally fast and always fresh, but write latency is penalty-driven. (Write-Behind delays DB writing to improve latency).\n\nCache invalidation is notoriously difficult, especially in distributed systems where updates occur across multiple pods. Invalidation is complex when data relationships are deep (e.g., clearing a User object requires clearing associated feed lists), or when concurrent systems overwrite data out-of-sync. Techniques involve using short TTLs, Pub/Sub channel broadcasts mapping to cache keys, or using a CDC (Change Data Capture) system like Debezium.',
    keyPoints: [
      'Cache-Aside: App handles fetching DB and loading Cache',
      'Read/Write-Through: Cache abstracts the DB completely for the App',
      'Write-Through adds write latency but ensures strict consistency',
      'Invalidation requires robust TTL logic or Pub-Sub sweeping',
      'CDC (Change Data Capture) is the modern way to sync Db to Cache out-of-band',
    ],
    followUps: [
      'What is the difference between Memcached and Redis?',
      'How does a Cache Stampede (Thundering Herd) occur, and how do you prevent it?',
    ],
    difficulty: 'Medium',
    companies: ['facebook', 'discord', 'twitter', 'bloomberg'],
    roles: ['backend', 'fullstack'],
    tags: ['cache', 'redis', 'system-design', 'scaling'],
    xpReward: 15,
  },
  {
    id: 'tech-sys-007',
    category: 'technical',
    subcategory: 'system-design',
    title: 'Scaling an Application — Zero to Millions',
    question: 'A monolithic web application hits vertical scaling limits. Outline the evolutionary steps and architectural components you would introduce to scale it to handle millions of requests.',
    idealAnswer: 'Scaling is an evolutionary process. First, place a Load Balancer (Nginx/ALB) and spin up multiple stateless instances of the application tier (Horizontal Scaling). Second, to handle database bottlenecks, introduce a distributed caching layer (Redis) to absorb Read traffic. \n\nThird, separate the web app from background tasks using Message Queues (SQS/RabbitMQ) and worker pools. Fourth, offload static assets (images/CSS) to a CDN (CloudFront).\n\nFinally, for the Database tier, isolate traffic using Read Replicas (for analytical/read load). If write load peaks, implement Database Sharding to distribute data across multiple servers. If the codebase becomes too unwieldy, strictly decouple functional domains into Microservices.',
    keyPoints: [
      'Add Load Balancers and horizontally scale application pods (Stateless tier)',
      'Add Caching (Redis/Memcached) for Read-heavy loads',
      'Offload static media to CDN (Content Delivery Network)',
      'Introduce Message Queues for async background tasks',
      'Database Scaling via Read Replicas, then eventual Sharding',
    ],
    followUps: [
      'What makes an application layer "stateless"? What happens to user sessions?',
      'What are the extreme downsides to Database Sharding?',
    ],
    difficulty: 'Hard',
    companies: ['amazon', 'startups', 'uber', 'meta'],
    roles: ['backend', 'system-design', 'fullstack'],
    tags: ['scaling', 'architecture', 'hld'],
    xpReward: 25,
  },
  {
    id: 'tech-sys-008',
    category: 'technical',
    subcategory: 'system-design',
    title: 'Consistent Hashing in Data Splitting',
    question: 'What is Consistent Hashing, and what exact problem does it solve in distributed caching or databases?',
    idealAnswer: 'In a standard hash-based partitioning scheme (using `hash(key) % N` where N is the number of servers), adding or removing a single server drastically changes the mapping for almost every key. This results in massive cache misses or data migrations across the whole cluster.\n\nConsistent Hashing solves this by mapping both servers and keys onto a virtual circular ring structure. A key is assigned to the first server it encounters clockwise on the ring. When a server is added or removed, only the keys belonging to its immediate neighbor are reassigned, leaving the vast majority of the mappings intact. To prevent uneven distribution (clusters forming on the ring), each physical server is represented by multiple "Virtual Nodes" scattered across the ring.',
    keyPoints: [
      'Standard hashing `mod N` breaks completely when N changes',
      'Maps servers and data keys to a continuous circular hash ring',
      'Data attaches to the next clockwise server on the ring',
      'Adding/removing servers only impacts local adjacent segments, not the whole cluster',
      'Virtual Nodes ensure balanced data distribution, avoiding hotspots',
    ],
    followUps: [
      'What databases internally utilize persistent hashing rings? (e.g., Cassandra/Riak)',
      'How do you manage replication on a consistent hash ring?',
    ],
    difficulty: 'Medium',
    companies: ['google', 'netflix', 'lyft'],
    roles: ['backend', 'system-design'],
    tags: ['system-design', 'hashing', 'cache', 'algorithms'],
    xpReward: 15,
  },
  {
    id: 'tech-sys-009',
    category: 'technical',
    subcategory: 'system-design',
    title: 'Designing Real-time Collaborative Editing',
    question: 'How would you design a real-time collaborative application like Google Docs? Focus on how conflicts are resolved when multiple users edit the exact same sentence simultaneously.',
    idealAnswer: 'Real-time collaboration depends on WebSockets for persistent, low-latency duplex connections. For conflict resolution, standard locking mechanisms are too rigid. \n\nWe typically use Operational Transformation (OT) or Conflict-free Replicated Data Types (CRDTs). In OT (used by Google Docs), the central server acts as the source of truth; when two clients send divergent operations concurrently, the server transforms the operations logically (e.g., "insert X at pos 5" becomes "insert X at pos 6" because a previous op inserted a character at pos 2) so all clients arrive at the exact same converged state.\n\nAlternatively, CRDTs merge operations mathematically without needing a centralized arbitration server, which is better for decentralized apps. The architecture involves an API gateway routing WS connections to Node.js/Go handlers, backed by an in-memory document state (Redis) syncing asynchronously to persistent storage.',
    keyPoints: [
      'Utilize WebSockets for bi-directional live communication',
      'Locking rows/files prevents fluid collaboration',
      'Use Operational Transformation (OT) for central-server arbitration',
      'Use CRDTs for localized, mathematical merge-convergence without a central brain',
      'High churn demands memory state (Redis) backing up to DB asynchronously',
    ],
    followUps: [
      'How do WebSockets differ from Long Polling or Server Sent Events?',
      'How does OT handle user cursors jumping around?',
    ],
    difficulty: 'Hard',
    companies: ['google', 'atlassian', 'microsoft', 'figma'],
    roles: ['frontend', 'backend', 'system-design'],
    tags: ['system-design', 'websockets', 'crdt', 'collaboration'],
    xpReward: 25,
  },

  // ─── OOP & Design Patterns (8) ───
  {
    id: 'tech-oop-001',
    category: 'technical',
    subcategory: 'oop',
    title: 'The SOLID Principles',
    question: 'Explain the 5 SOLID principles of Object-Oriented Design. Give a practical example of the Open-Closed Principle.',
    idealAnswer: 'SOLID ensures maintainable software. \nS = Single Responsibility: A class should have only one reason to change. \nO = Open-Closed: Software entities should be open for extension, but closed for modification. \nL = Liskov Substitution: Subclasses must be substitutable for their base classes without breaking the app. \nI = Interface Segregation: Many client-specific interfaces are better than one general-purpose interface. \nD = Dependency Inversion: Depend upon abstractions (interfaces), not concretions.\n\nFor Open-Closed, imagine a `DiscountCalculator` class with an if/else checking `isVIP`. Rather than modifying the calculator every time a new tier is added, we make it accept an `IDiscountPolicy` interface. We extend the system by creating a new `SeasonalDiscount` class implementing the interface, leaving the Calculator class closed and untested.',
    keyPoints: [
      'Single Responsibility (1 reason to change)',
      'Open-Closed (Open to extend, closed to modify)',
      'Liskov Substitution (Child can identically replace Parent)',
      'Interface Segregation (Split fat interfaces into small ones)',
      'Dependency Inversion (Depend on abstractions, use Dependency Injection)',
    ],
    followUps: [
      'How does Dependency Injection facilitate the \'D\' in SOLID?',
      'Give an example where Liskov Substitution is broken (e.g., Square extending Rectangle).',
    ],
    difficulty: 'Medium',
    companies: ['microsoft', 'apple', 'amazon', 'enterprise'],
    roles: ['sde', 'backend', 'frontend'],
    tags: ['oop', 'solid', 'design-patterns', 'architecture'],
    xpReward: 15,
  },
  {
    id: 'tech-oop-002',
    category: 'technical',
    subcategory: 'oop',
    title: 'Singleton Pattern — Pros and Cons',
    question: 'How do you implement a Thread-Safe Singleton? Why is the Singleton pattern often considered an "anti-pattern" in modern testing and architecture?',
    idealAnswer: 'A Singleton ensures a class has only one instance and provides a global access point. In languages like Java, a thread-safe implementation uses "Double-Checked Locking" with the `volatile` keyword, or simply instantiates eagerly at class load time, or using an Enum.\n\nIt is often dubbed an anti-pattern because: 1) It introduces global state, making components tightly coupled. 2) It makes unit testing extremely difficult since global state bleeds across test cases. 3) It hides dependencies inside the code instead of exposing them via constructors. Modern architectures avoid explicit Singletons; instead, they use Dependency Injection frameworks (like Spring or Dagger) to manage and inject a single instance at the application startup level.',
    keyPoints: [
      'Ensures single instance, global access point',
      'Thread safety requires volatile sync, or Double-Checked Locking',
      'Anti-pattern: creates untrackable global state',
      'Anti-pattern: heavily damages unit testability (state bleed)',
      'Modern mitigation: Use Dependency Injection lifecycle scopes instead',
    ],
    followUps: [
      'Write the pseudocode for a generic Double-Checked Locking Singleton.',
      'How does state bleeding affect concurrent unit tests?',
    ],
    difficulty: 'Easy',
    companies: ['adobe', 'salesforce', 'lyft'],
    roles: ['sde', 'backend'],
    tags: ['design-patterns', 'singleton', 'oop'],
    xpReward: 10,
  },
  {
    id: 'tech-oop-003',
    category: 'technical',
    subcategory: 'oop',
    title: 'Factory vs Abstract Factory',
    question: 'Explain the difference between a Factory Method and an Abstract Factory pattern. When would you use one over the other?',
    idealAnswer: 'The Factory Method pattern defines an interface for creating one single type of object, but lets subclasses alter the type of object that will be created. It uses inheritance and relies on a method to handle instantiation (e.g., `Document.createPage()`).\n\nThe Abstract Factory pattern is a super-factory. It provides an interface for creating families of related or dependent objects without specifying their concrete classes. It uses object composition.\n\nUse a Factory Method when you have one specific product to create but want to delegate the creation logic. Use an Abstract Factory when your system must support multiple families of products (e.g., a cross-platform UI builder that needs to generate WindowsButtons + WindowsMenus vs MacButtons + MacMenus cohesively).',
    keyPoints: [
      'Factory Method builds ONE product, uses inheritance',
      'Abstract Factory builds families of related products, uses composition',
      'Factory: "Create A Button"',
      'Abstract Factory: "Create a Mac-Style Environment (Button, Scrollbar, Window)"',
      'Both decouple object creation from object usage',
    ],
    followUps: [
      'How does the Builder pattern differ from the Factory pattern?',
      'Is the Factory pattern an application of the Open-Closed Principle?',
    ],
    difficulty: 'Medium',
    companies: ['microsoft', 'enterprise', 'apple'],
    roles: ['sde', 'frontend', 'backend'],
    tags: ['design-patterns', 'factory', 'oop'],
    xpReward: 15,
  },
  {
    id: 'tech-oop-004',
    category: 'technical',
    subcategory: 'oop',
    title: 'Strategy vs State Pattern',
    question: 'Both Strategy and State patterns encapsulate algorithms/behaviors dynamically. What is the fundamental difference in their intent and how they change state?',
    idealAnswer: 'Structurally, they look identical (Composition over Inheritance). However, their intent differs completely.\n\nThe Strategy Pattern allows a client to choose an algorithm at runtime. The client provides the context and tells the object how to behave. (e.g., a Navigation App where the user chooses `WalkingStrategy` vs `DrivingStrategy`). Strategies are completely independent of each other.\n\nThe State Pattern allows an object to alter its behavior when its internal state changes. The transition logic is internal to the states themselves. The client does not know about the states. (e.g., a `VendingMachine` moving internally from `NoCoinState` to `HasCoinState` to `DispensingState`). States are heavily aware of context transitions.',
    keyPoints: [
      'Identical structure, entirely different intent',
      'Strategy: Client selects the behavior dynamically (e.g., Sorting Algorithms)',
      'State: Object transitions itself internally based on rules (e.g., State Machine)',
      'Strategies are unaware of each other',
      'States are aware of each other and drive the transitions',
    ],
    followUps: [
      'How do you handle cyclic dependencies in the State pattern?',
      'Does the Strategy pattern help eliminate large Switch/Case statements?',
    ],
    difficulty: 'Hard',
    companies: ['amazon', 'atlassian', 'stripe'],
    roles: ['sde', 'backend'],
    tags: ['design-patterns', 'strategy', 'state', 'oop'],
    xpReward: 25,
  },
  {
    id: 'tech-oop-005',
    category: 'technical',
    subcategory: 'oop',
    title: 'Composition over Inheritance',
    question: 'What does "Favor Composition over Inheritance" mean? Why do deeply nested class inheritances cause architectural problems?',
    idealAnswer: '"Favor Composition over Inheritance" means designing your classes by combining smaller, distinct objects specifying behaviors (Has-A relationship) rather than building rigid, deep class hierarchies (Is-A relationship).\n\nDeep inheritance causes the "Fragile Base Class Problem"—modifying a parent class breaks all subclasses. It also locks you into static designs; multiple inheritance is avoided in many languages (like Java) due to the Diamond Problem. \n\nComposition provides flexibility. If you have a `Car`, inheriting from `Vehicle` vs `Machine` is rigid. Instead, a `Car` HAS-A `EngineInterface`. At runtime, you can swap a `GasEngine` for an `ElectricEngine` effortlessly. It makes testing easier via mocking and adheres better to the Single Responsibility Principle.',
    keyPoints: [
      'Inheritance is rigid (Is-A); Composition is flexible (Has-A)',
      'Deep inheritance leads to the Fragile Base Class problem',
      'Prevents monolithic, tightly coupled hierarchies',
      'Composition allows swapping behaviors dynamically at runtime',
      'Composition makes unit testing significantly easier (mocking interfaces)',
    ],
    followUps: [
      'What is a mixin or trait, and how does it relate to this?',
      'When is Inheritance actually the correct choice?',
    ],
    difficulty: 'Medium',
    companies: ['google', 'meta', 'spotify'],
    roles: ['sde', 'frontend', 'backend'],
    tags: ['oop', 'architecture', 'best-practices'],
    xpReward: 15,
  },
  {
    id: 'tech-oop-006',
    category: 'technical',
    subcategory: 'oop',
    title: 'The Observer Pattern and Event Loops',
    question: 'Explain the Observer Pattern. How is it fundamentally implemented, and how does it map to pub-sub systems or UI event listeners?',
    idealAnswer: 'The Observer Pattern defines a one-to-many relationship where a Subject maintains a list of Observers. When the Subject’s state changes, it iterates through its list and calls an `update()` method on all registered observers. It decouples the object generating the event from the objects reacting to it.\n\nIn UI architecture (like the DOM), event listeners act exactly as Observers attached to UI Subjects (buttons). A Pub-Sub system (like Kafka) is an evolution of Observer, adding a message broker channel between publishers and subscribers, offering complete asynchronous decoupling whereas classic Observer is typically synchronous class method calls.',
    keyPoints: [
      'One Producer (Subject), Many Consumers (Observers)',
      'Decouples logic; subjects don\'t care what the observers are doing',
      'Classic implementation maintains a List of interface-bound Observers',
      'Core mechanic behind UI Event Listeners (OnClick)',
      'Pub-Sub is an architectural scaling of this pattern',
    ],
    followUps: [
      'What is memory leak risk associated with Observers? (Lapsed Listener Problem)',
      'How does RxJS or Reactive Programming extend the Observer pattern?',
    ],
    difficulty: 'Easy',
    companies: ['netflix', 'apple', 'discord'],
    roles: ['frontend', 'sde'],
    tags: ['design-patterns', 'observer', 'events', 'oop'],
    xpReward: 10,
  },
  {
    id: 'tech-oop-007',
    category: 'technical',
    subcategory: 'oop',
    title: 'Access Modifiers and Encapsulation',
    question: 'How do access modifiers (public, private, protected) enforce Encapsulation? When should you expose state via Getter/Setter properties vs mutating behaviors?',
    idealAnswer: 'Access modifiers enforce Encapsulation by hiding the internal state of an object, preventing arbitrary external modification. `private` binds data to the class itself, `protected` opens it to subclasses, and `public` opens it globally.\n\nHowever, mindless Getters/Setters break encapsulation by exposing internal data shapes. True encapsulation exposes Behaviors, not Data. For example, instead of `bankAccount.setBalance(bankAccount.getBalance() - 100);` (which exposes data structure and lacks thread safety), you provide a behavior: `bankAccount.withdraw(100);`. Getters/Setters should only be used in pure data-transfer objects (DTOs) or when specific validation is required on access.',
    keyPoints: [
      'Modifiers prevent unauthorized state corruption',
      'Private restricts to class, Protected to hierarchy, Public everywhere',
      'Mindless Getters/Setters break object-oriented behavior boundaries',
      'Design should expose verbs (Behaviors) rather than nouns (Data representations)',
      'DTOs (Data Transfer Objects) are the exception for pure data access',
    ],
    followUps: [
      'Explain the "Tell, Don’t Ask" OOP principle.',
      'How does immutability affect your encapsulation strategy?',
    ],
    difficulty: 'Medium',
    companies: ['enterprise', 'startups', 'amazon'],
    roles: ['sde', 'backend'],
    tags: ['oop', 'encapsulation', 'basics'],
    xpReward: 15,
  },
  {
    id: 'tech-oop-008',
    category: 'technical',
    subcategory: 'oop',
    title: 'The Decorator Pattern',
    question: 'Describe the Decorator Pattern. How does it provide a flexible alternative to subclassing for extending functionality?',
    idealAnswer: 'The Decorator Pattern attaches additional responsibilities to an object dynamically at runtime. Instead of creating massive permutations of Subclasses (e.g., `DarkRoastWithMilkAndSugar`, `EspressoWithWhip`), you create decorator wrappers that implements the same interface as the core object, but holds a reference to it.\n\nYou pass the core object into the decorator, and the decorator delegates calls to the core object while adding its own logic before or after. Because decorators share the core interface, you can stack infinite decorators infinitely without touching the core classes. It heavily leverages Composition to satisfy the Open-Closed Principle.',
    keyPoints: [
      'Dynamically adds behaviors at runtime without subclass explosion',
      'Uses wrapping/composition, but maintains the target interface',
      'Decorators can be stacked recursively',
      'Commonly used in Java I/O streams (BufferedInputStream wraps FileInputStream)',
      'Satisfies the Open-Closed Principle beautifully',
    ],
    followUps: [
      'Explain the difference between Decorator and Proxy patterns.',
      'How do Python or TypeScript @ decorators abstract this concept?',
    ],
    difficulty: 'Medium',
    companies: ['microsoft', 'google', 'spotify'],
    roles: ['sde', 'frontend', 'backend'],
    tags: ['design-patterns', 'decorator', 'oop'],
    xpReward: 15,
  },

  // ─── Database & SQL (6) ───
  {
    id: 'tech-db-001',
    category: 'technical',
    subcategory: 'database',
    title: 'ACID Properties in RDBMS',
    question: 'Define the ACID properties of a database transaction. How does a database implement Durability and Isolation?',
    idealAnswer: 'ACID stands for Atomicity (all or nothing operations), Consistency (data starts/ends in valid states based on constraints), Isolation (concurrent transactions do not interfere), and Durability (committed data survives crashes).\n\nDatabases implement Durability using Write-Ahead Logging (WAL). Before modifying actual table storage, the DB appends the transaction details to a durable disk log. If a crash occurs, the DB replays the WAL to recover state.\nIsolation is implemented using MVCC (Multi-Version Concurrency Control) or lock managers. MVCC retains multiple versions of rows so read operations never block write operations, granting snapshotted views to concurrently running transactions based on their isolation level.',
    keyPoints: [
      'Atomicity, Consistency, Isolation, Durability defined',
      'Atomicity uses transaction blocks to rollback failures',
      'Durability is achieved using Write-Ahead Logging (WAL) on disk',
      'Isolation manages concurrency and dirty/phantom reads',
      'Isolation is implemented via Locks or MVCC (Multi-Version Concurrency Control)',
    ],
    followUps: [
      'Explain the 4 standard Database Isolation Levels.',
      'What is a Dirty Read?',
    ],
    difficulty: 'Medium',
    companies: ['stripe', 'amazon', 'uber', 'capital-one'],
    roles: ['backend', 'data'],
    tags: ['database', 'acid', 'sql', 'transactions'],
    xpReward: 15,
  },
  {
    id: 'tech-db-002',
    category: 'technical',
    subcategory: 'database',
    title: 'Clustered vs Non-Clustered Indexes',
    question: 'Explain the difference between Clustered and Non-Clustered Indexes. How do they affect read and write performance?',
    idealAnswer: 'A Clustered Index dictates the physical sorting order of the data rows on the disk (typically a B-Tree). Because data can only be physically sorted one way, there can be only one clustered index per table (usually the Primary Key). Reads are incredibly fast because the leaf nodes ARE the data.\n\nA Non-Clustered index creates a separate copy of the selected columns, alongside a pointer back to the actual data row. A table can have multiple non-clustered indexes. \nReads speed up significantly due to fast lookup, but both index types severely penalize Write performance. Every INSERT/UPDATE requires reorganizing the index trees. Over-indexing wastes disk space and slows down ingestion pipelines.',
    keyPoints: [
      'Clustered Index alters physical disk layout (max 1 per table)',
      'Non-Clustered Index creates separate structure with pointers',
      'Clustered reads are faster (data is at the leaf node)',
      'Both index architectures use B-Trees for O(log N) lookup',
      'Indexes drastically slow down INS/UPD/DEL ops due to tree rebalancing',
    ],
    followUps: [
      'What is a Covering Index?',
      'How does a Hash Index differ from a B-Tree Index?',
    ],
    difficulty: 'Easy',
    companies: ['microsoft', 'google', 'meta', 'bloomberg'],
    roles: ['backend', 'data', 'sde'],
    tags: ['database', 'sql', 'indexes', 'performance'],
    xpReward: 10,
  },
  {
    id: 'tech-db-003',
    category: 'technical',
    subcategory: 'database',
    title: 'Handling N+1 Query Problems',
    question: 'What is the N+1 Query Problem encountered in ORMs, and how do you resolve it?',
    idealAnswer: 'The N+1 problem occurs when an application executes 1 query to fetch a list of N parent records, and then iterates through them executing an additional query for each record to fetch child relations. Fetching 100 users results in 101 total database queries, causing severe network latency and DB strain.\n\nThis is a common flaw in ORMs (like Hibernate or Prisma) defaulting to Lazy Loading. To resolve it, we implement Eager Loading. This instructs the ORM to execute a `JOIN` to fetch all data at once, or use query batching (the `IN` clause) to fetch all parents in query 1, and all associated children in query 2 (100 users results in just 2 queries). In GraphQL architectures, the DataLoader pattern solves this via batching.',
    keyPoints: [
      'Occurs when fetching parent lists, then querying independently per child',
      'Causes massive network round trips scaling with N rows',
      'Driven by naive ORM Lazy Loading defaults',
      'Fix: Eager loading via SQL JOINs',
      'Fix: Batch queries using the IN () clause or DataLoaders',
    ],
    followUps: [
      'How does the GraphQL DataLoader batch asynchronous queries under the hood?',
      'Is Eager Loading universally better than Lazy Loading?',
    ],
    difficulty: 'Medium',
    companies: ['spotify', 'airbnb', 'github'],
    roles: ['backend', 'fullstack'],
    tags: ['database', 'orm', 'sql', 'optimization'],
    xpReward: 15,
  },
  {
    id: 'tech-db-004',
    category: 'technical',
    subcategory: 'database',
    title: 'SQL Normalization Forms (1NF to 3NF)',
    question: 'Walk me through Database Normalization from 1st to 3rd Normal Form. Why do systems like Data Warehouses heavily Denormalize data?',
    idealAnswer: 'Normalization reduces data redundancy and prevents update anomalies. \n1NF requires atomic values (no comma-separated lists) and unique rows. \n2NF requires 1NF, plus all non-key columns must completely depend on the entire primary key (stops partial dependency in composite keys). \n3NF requires 2NF, plus all non-key columns must depend strictly on the primary key, and not on other non-key columns (stops transitive dependencies).\n\nDenormalization intentionally reintroduces redundancy. Data warehouses (Snowflake, BigQuery) do this because analytical queries aggregate massive datasets across huge dimensions. JOINs are incredibly expensive at petabyte scale. Pre-joining and flattening data into wide tables makes Read operations drastically faster, which is the primary goal of OLAP analytics.',
    keyPoints: [
      'Goal: Eliminate duplication, reduce insertion/deletion anomalies',
      '1NF: Atomic columns, tabular structure',
      '2NF: Removes partial dependencies (applies to composite keys)',
      '3NF: Removes transitive dependencies (non-keys rely on other non-keys)',
      'Denormalization (OLAP) speeds up heavy analytical reads at the cost of disk space and write complexity',
    ],
    followUps: [
      'Explain the Star Schema typically used in Data Warehouses.',
      'What is Boyce-Codd Normal Form (BCNF)?',
    ],
    difficulty: 'Medium',
    companies: ['data-focused', 'amazon', 'financial-banks'],
    roles: ['data', 'backend'],
    tags: ['database', 'sql', 'normalization', 'design'],
    xpReward: 15,
  },
  {
    id: 'tech-db-005',
    category: 'technical',
    subcategory: 'database',
    title: 'Optimistic vs Pessimistic Locking',
    question: 'How do you handle concurrent database writes? Explain the difference between Optimistic and Pessimistic concurrency control.',
    idealAnswer: 'Both handle racing concurrent writes. \nPessimistic Locking assumes conflicts WILL happen. The DB places a strict transaction lock on the row (e.g., `SELECT ... FOR UPDATE`). No other transaction can read or update that row until the lock releases. This is safe, but degrades performance heavily and risks Deadlocks in high contention scenarios.\n\nOptimistic Locking assumes conflicts are RARE. No database locks are used. Instead, a `version` or `updated_at` column is added. When updating, the app checks if the version matches the one it originally read (`UPDATE table SET val=new, version=2 WHERE id=1 AND version=1`). If the row was altered by another thread, the row match fails, the update aborts, and the application layer handles the retry logic. Optimistic is faster and deadlock-free.',
    keyPoints: [
      'Pessimistic uses hard DB locks (FOR UPDATE); blocks other threads',
      'Pessimistic causes read/write bottlenecks and risks deadlocks',
      'Optimistic uses no locks; relies on Versioning or Timestamp columns',
      'Optimistic fails updates gracefully if version hashes don\'t match',
      'Optimistic is best for heavy read/low collision; Pessimistic for high collision financial ops',
    ],
    followUps: [
      'How does MVCC (Multi-Version Concurrency Control) implement optimistic concepts?',
      'If an optimistic lock fails, how should the backend application respond?',
    ],
    difficulty: 'Hard',
    companies: ['stripe', 'paypal', 'robinhood', 'coinbase'],
    roles: ['backend', 'system-design'],
    tags: ['database', 'concurrency', 'locking', 'sql'],
    xpReward: 25,
  },
  {
    id: 'tech-db-006',
    category: 'technical',
    subcategory: 'database',
    title: 'Explain Plan and Query Tuning',
    question: 'A large SQL query is running unbearably slowly in production. Walk me through exactly how you use EXPLAIN analyze to track down the bottleneck and the common strategies you use to fix it.',
    idealAnswer: 'I would prefix the exact query with `EXPLAIN ANALYZE` to map the execution plan. The Database Engine will return a node tree of the plan.\nI look specifically for "Sequential Scans" (Full Table Scans) on massive tables, which indicates missing indexes or non-sargable WHERE clauses (e.g., using `LIKE \'%term\'` or function wrappers that defeat index usage).\nI check the JOIN algorithms. If it resorts to a Nested Loop over millions of rows rather than a Hash Join, the DB lacks indexed join keys.\nTo fix it: I create B-Tree indexes on the filtering columns, replace subqueries with JOINs or CTEs, utilize Covering Indexes to prevent row lookups, and ensure the query isn\'t extracting `SELECT *` but only required columns.',
    keyPoints: [
      'Use EXPLAIN / EXPLAIN ANALYZE to view the execution optimizer tree',
      'Hunt for Sequential Scans (Full Table Scans) which denote missing Indexes',
      'Check for slow Nested Loop joins vs efficient Hash Joins',
      'Check for non-SARGable clauses (functions or wildcards that break indexing)',
      'Fix via B-Tree indexing, partition pruning, or removing SELECT *',
    ],
    followUps: [
      'What is a Covering Index and how does it prevent DB heap lookups?',
      'When might the SQL Engine intentionally choose a Full Table Scan even if an index exists?',
    ],
    difficulty: 'Medium',
    companies: ['netflix', 'github', 'salesforce'],
    roles: ['backend', 'data'],
    tags: ['database', 'sql', 'performance', 'indexing'],
    xpReward: 15,
  },

  // ─── OS & Concurrency (4) ───
  {
    id: 'tech-os-001',
    category: 'technical',
    subcategory: 'os',
    title: 'Processes vs Threads',
    question: 'Explain the difference between a Process and a Thread. How do they share memory, and what are the overheads associated with context switching?',
    idealAnswer: 'A Process is an independently executing instance of a program. It possesses its own isolated memory space, file descriptors, and security context. Because processes are isolated, communicating between them requires heavy Inter-Process Communication (IPC) pipelines.\n\nA Thread is a lightweight unit of execution within a Process. All threads within a single process share the same heap memory, data segment, and file descriptors, though each keeps its own stack and registers.\nContext switching between processes is very expensive because the OS must flush CPU caches and swap memory maps/TLBs. Context switching between threads of the same process is much faster because the memory space remains identical.',
    keyPoints: [
      'Process: Isolated memory, independent execution, heavy context switch',
      'Thread: Shared heap/memory within a process, lightweight context switch',
      'Threads have individual call stacks and registers',
      'Inter-Process Communication (IPC) is complex and requires OS intervention',
      'Threads are highly vulnerable to race conditions due to shared memory',
    ],
    followUps: [
      'What happens if one thread crashes in a multithreaded process vs one process crashing in a multi-process architecture?',
      'Explain the Python GIL (Global Interpreter Lock).',
    ],
    difficulty: 'Easy',
    companies: ['meta', 'microsoft', 'amazon'],
    roles: ['sde', 'backend', 'system-design'],
    tags: ['os', 'concurrency', 'threads', 'basics'],
    xpReward: 10,
  },
  {
    id: 'tech-os-002',
    category: 'technical',
    subcategory: 'os',
    title: 'Mutex, Semaphore, and Deadlock',
    question: 'Define Mutexes and Semaphores. Then explain the four Coffman conditions required for a Deadlock to occur.',
    idealAnswer: 'A Mutex (Mutual Exclusion) acts as a strictly binary lock allowing only one thread access to a critical section of code. The thread that acquires the mutex is the only one that can release it.\nA Semaphore acts as a signaling counter allowing a predefined number (N) of threads access to a pool of resources. \n\nA Deadlock occurs when threads are permanently frozen, waiting on locks held by each other. It requires 4 conditions simultaneously:\n1. Mutual Exclusion: Resources cannot be shared.\n2. Hold and Wait: A thread holding a resource demands another.\n3. No Preemption: Resources cannot be forcibly taken away by the OS.\n4. Circular Wait: Thread A waits on B, B waits on C, C waits on A.',
    keyPoints: [
      'Mutex: Binary lock, owner thread must unlock it',
      'Semaphore: Counter signaling available resources',
      'Deadlock C1: Mutual Exclusion',
      'Deadlock C2: Hold and Wait',
      'Deadlock C3: No Preemption',
      'Deadlock C4: Circular Wait (the most preventable condition)',
    ],
    followUps: [
      'How can you architect your locking strategy to definitively prevent Deadlocks? (Lock Ordering)',
      'What is a Spinlock?',
    ],
    difficulty: 'Medium',
    companies: ['google', 'microsoft', 'nvidia'],
    roles: ['backend', 'sde'],
    tags: ['os', 'concurrency', 'deadlock', 'locking'],
    xpReward: 15,
  },
  {
    id: 'tech-os-003',
    category: 'technical',
    subcategory: 'os',
    title: 'Asynchronous I/O and Event Loops',
    question: 'How do runtimes like Node.js achieve massive concurrency using a single thread? Contrast non-blocking I/O with traditional blocking thread-per-request models.',
    idealAnswer: 'Traditional servers (like early Apache/Java) use a thread-per-request model. When a request hits a database, the thread blocks (sleeps). To handle 10,000 requests, you need 10,000 OS threads, which consumes massive RAM and kills CPU via context switching.\n\nNode.js and modern Go/Python AsyncIO use a single-threaded Event Loop combined with non-blocking I/O. When a file or network request is made, the main thread offloads the wait to the OS kernel (via epoll/kqueue) and immediately picks up the next task. When the OS finishes the background I/O, it pushes a callback to the Event Queue. The Event Loop eventually pops that callback back onto the main thread. This architecture efficiently handles millions of concurrent idle connections without OS thread overhead.',
    keyPoints: [
      'Traditional: Thread-per-request blocks memory and cpu context',
      'AsyncIO uses a single executing Event Loop architecture',
      'Heavy waiting (network/disk) is delegated to OS kernel threads/notifications',
      'Callbacks resume execution without holding thread context during I/O sleep',
      'Ideal for heavy I/O workloads, but single-thread chokes on heavy CPU calculations',
    ],
    followUps: [
      'If Node.js is single-threaded, how does it handle CPU-intensive tasks like image processing?',
      'What is epoll or kqueue at the OS level?',
    ],
    difficulty: 'Medium',
    companies: ['netflix', 'discord', 'uber'],
    roles: ['backend', 'frontend', 'sde'],
    tags: ['os', 'concurrency', 'async', 'nodejs'],
    xpReward: 15,
  },
  {
    id: 'tech-os-004',
    category: 'technical',
    subcategory: 'os',
    title: 'Virtual Memory and Paging',
    question: 'Explain how Virtual Memory works. What problem does it solve, and what is a Page Fault?',
    idealAnswer: 'Virtual Memory abstracts the system RAM. The OS provides every process the illusion that it controls a massive, continuous block of memory. In reality, the OS divides memory into blocks called Pages (usually 4KB), mapping Virtual Addresses to disparate Physical RAM frames via a Page Table.\n\nThis solves two problems: 1) It isolates processes securely, preventing them from reading each other\'s RAM. 2) It allows execution of programs larger than physical memory by swapping inactive pages out to the disk.\n\nA Page Fault occurs when a process attempts to access a virtual memory address whose corresponding physical page is not currently in RAM (it was swapped to disk). The OS must freeze the thread, load the page from slow disk into RAM, update the Page Table, and resume the thread.',
    keyPoints: [
      'Abstracts physical RAM into continuous virtual spaces tailored per process',
      'Solves security/isolation via address spaces',
      'Allows running programs larger than physical RAM via Disk Swapping',
      'Translation utilizes hardware TLB and OS Page Tables',
      'A Page Fault halts execution to fetch discarded data logically from disk',
    ],
    followUps: [
      'What is "Thrashing" in relation to virtual memory?',
      'How does the CPU Translation Lookaside Buffer (TLB) accelerate this process?',
    ],
    difficulty: 'Hard',
    companies: ['apple', 'intel', 'google'],
    roles: ['sde', 'backend'],
    tags: ['os', 'memory', 'paging', 'hardware'],
    xpReward: 25,
  },

  // ─── Networking & Web (3) ───
  {
    id: 'tech-net-001',
    category: 'technical',
    subcategory: 'networking',
    title: 'TCP vs UDP Protocols',
    question: 'Compare TCP and UDP protocols. How does TCP establish a connection, and when would you explicitly choose UDP?',
    idealAnswer: 'TCP (Transmission Control Protocol) is connection-oriented and reliable. It establishes connections via a 3-Way Handshake (SYN → SYN-ACK → ACK). It ensures data sequencing, error checking, and flow/congestion control, retransmitting any dropped packets. It is used for Web (HTTP), Email, and File Transfers.\n\nUDP (User Datagram Protocol) is connectionless and unreliable. It fires packets without handshakes, meaning pieces can arrive out of order or disappear entirely. However, it is significantly faster due to the lack of overhead.\nYou choose UDP when raw speed is paramount and occasional data loss is acceptable. It is the standard for VoIP, live video streaming, and fast-paced multiplayer gaming architectures.',
    keyPoints: [
      'TCP: Reliable, ordered, heavy overhead, uses 3-Way Handshake',
      'TCP guarantees delivery via retransmission mechanics',
      'UDP: Unreliable, unordered, lightweight, stateless',
      'UDP does not require handshakes or connection confirmation',
      'UDP used for live Video, Audio, and Gaming where speed > precision',
    ],
    followUps: [
      'Explain the "Head of Line Blocking" problem in TCP.',
      'How does HTTP/3 (QUIC) utilize UDP to solve TCP bottleneck issues?',
    ],
    difficulty: 'Easy',
    companies: ['cloudflare', 'discord', 'netflix', 'amazon'],
    roles: ['backend', 'sde', 'fullstack'],
    tags: ['networking', 'protocols', 'tcp-udp'],
    xpReward: 10,
  },
  {
    id: 'tech-net-002',
    category: 'technical',
    subcategory: 'networking',
    title: 'DNS Resolution architecture',
    question: 'What exactly happens at the networking layer when a user types a URL in their browser? Focus on the DNS resolution chain.',
    idealAnswer: 'When a URL is entered, the browser must map the human-readable domain to an IP address.\n1. It checks the Browser Cache, OS Cache, then the local Router Cache.\n2. If empty, it queries the ISP\'s Recursive Resolver.\n3. The Resolver contacts the DNS Root Nameserver to find the applicable TLD (Top-Level Domain) server (e.g., the `.com` server).\n4. The TLD Server directs the resolver to the Authoritative Nameserver (like AWS Route53 or Cloudflare) hosting the domain records.\n5. The Authoritative Server returns the requested A Record (IPv4) or AAAA Record (IPv6).\n\nWith the IP resolved, the browser initiates the TCP 3-Way Handshake, establishes TLS/SSL for HTTPS security, and finally fires the HTTP GET request.',
    keyPoints: [
      'Check local Browser, OS, and Router caches first',
      'If missed, query ISP Recursive Resolver',
      'Chain: Root Server → TLD Server (.com) → Authoritative Server',
      'Authoritative Server returns the mapped A Record (IP address)',
      'Followed by TCP Handshake and TLS encryption layers',
    ],
    followUps: [
      'What is a CNAME record and how does it differ from an A record?',
      'How does DNS handle Geographic load balancing?',
    ],
    difficulty: 'Medium',
    companies: ['cloudflare', 'google', 'meta', 'aws'],
    roles: ['backend', 'frontend', 'sde'],
    tags: ['networking', 'dns', 'web-architecture'],
    xpReward: 15,
  },
  {
    id: 'tech-net-003',
    category: 'technical',
    subcategory: 'networking',
    title: 'CORS Strategies and Security',
    question: 'Explain Cross-Origin Resource Sharing (CORS). Why do browsers implement it, and how do preflight requests work?',
    idealAnswer: 'CORS is a browser security mechanism that restricts a webpage from making AJAX or Fetch requests to domains different from the one that served the webpage. By default, the Same-Origin Policy forbids these requests to prevent malicious scripts from executing cross-site data theft (e.g., scraping authenticated bank data).\n\nTo allow exceptions, the target server must explicitly send `Access-Control-Allow-Origin` HTTP headers authorizing the caller. \nFor complex requests (like PUT/DELETE or custom headers), the browser automatically sends a "Preflight" OPTIONS request to the server to verify permissions before sending the actual damaging payload. If the server approves the OPTIONS request headers, the browser executes the main request.',
    keyPoints: [
      'Browser security feature enforcing the Same-Origin Policy',
      'Prevents malicious scripts attacking foreign domains using local cookies/auth',
      'Server bypasses it by sending Access-Control headers authorizing the origin',
      'Complex operations trigger a preflight OPTIONS request automatically',
      'CORS is strictly a Browser-level enforcement, not an API security feature',
    ],
    followUps: [
      'Can a malicious user bypass CORS by using curl or Postman?',
      'Explain Cross-Site Scripting (XSS) and how it defeats same-origin boundaries.',
    ],
    difficulty: 'Medium',
    companies: ['meta', 'stripe', 'frontend-roles'],
    roles: ['frontend', 'fullstack'],
    tags: ['networking', 'cors', 'security', 'web'],
    xpReward: 15,
  }
];
